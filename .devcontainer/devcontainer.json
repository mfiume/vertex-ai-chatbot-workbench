{
  "name": "Vertex AI Chatbot for Verily Workbench",
  "image": "node:20-bullseye",

  "runArgs": [
    "--network=app-network",
    "--name=application-server"
  ],

  "forwardPorts": [3000],

  "portsAttributes": {
    "3000": {
      "label": "Vertex AI Chatbot",
      "onAutoForward": "notify"
    }
  },

  "containerEnv": {
    "VERTEX_AI_LOCATION": "us-central1",
    "VERTEX_AI_MODEL": "gemini-1.5-flash",
    "PORT": "3000"
  },

  "postCreateCommand": "apt-get update && apt-get install -y git-lfs curl && git lfs install",

  "postStartCommand": "/bin/bash /workspace/.devcontainer/post-startup.sh",

  "features": {
    "ghcr.io/devcontainers/features/gcloud:1": {
      "version": "latest"
    }
  }
}
